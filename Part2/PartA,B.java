int R = Integer.parseInt(args[1]); // Number of iterations for part A we simply pass 1 as our argument
//For part B, we simply pass R number of desired iterations as an argument
for (int iteration = 1; iteration <= R; iteration++) {
    // Configure and run MapReduce job
    // Update centroidsPath for next iteration
}